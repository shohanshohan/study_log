访问控制器时直接报错： variable type error： array 
把数组转换成json 
return json($data);
或在linux 下用 json_encode($data);
如果是接口访问，直接配置文件config.php 中配置：
// 默认输出类型
'default_return_type'    => 'json',
然后直接return 数组就行了，不用二次转换。
