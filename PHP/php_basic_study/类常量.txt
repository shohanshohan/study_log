类常量的值必须是一个定值，不能是变量，类属性，数学运算的结果或函数调用。用关键字 const
接口（interface）中也可以定义常量
如：
<?php
namespace app;
class A
{
  const CLASS_NAME = self::class; 
}

echo A::CLASS_NAME; //调用常量用静态调用
继承的子类可以重写父类的常量，如：
class B extends A
{
  const CLASS_NAME = 'undefined';
}
