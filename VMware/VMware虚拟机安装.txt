直接百度下载VMware最新版本，这里安装的是VMware v14.1.1
下载好后直接双击.exe文件进行安装，尽量不要安装在C盘
 进入安装步骤很简单，一直下不步就好了，最后要注意的是，这是个要产品密钥的产品
 这里提供几个密钥：
 VMware 2017 v14.x 永久许可证激活密钥
FF31K-AHZD1-H8ETZ-8WWEZ-WUUVA
CV7T2-6WY5Q-48EWP-ZXY7X-QGUWD
 最后一步选择输入证书，就是密钥进行安装，finish
 
 1）打开VMwear选择新建虚拟机
 
 2）选择自定义安装。
 
 3）虚拟机兼容性选择，如果是14.1的版本就选workstation 14.X
 
 4）选择稍后安装操作系统
 
 5）操作系统的选择，选择Linux，CentOs 64位
 
 6）虚拟机位置与命名，这个要根据自己需要自定义名称和路径，路径放自己好找的地方，比如放E盘VMware虚拟机/centos目录下
 
 7）处理器与内存的分配,处理器分配要根据自己的实际需求来分配。在使用过程中CPU不够的话是可以再增加的。
    “处理器数量”表示虚拟机使用的CPU个数，一般是1个；“每个处理器的核心数量”表示虚拟机使用的CPU是几核的（我这里是双核，选择2）；上面这两个参数请根据你的     物理   机实际情况进行选择，如果设置不正确会造成虚拟机卡顿、死机，我们可以使用默认参数；点击“下一步”；
    给虚拟机分配内存，内存越大，运行越流畅，但不能超出推荐的最大内存，内存也是要根据实际的需求分配。我的宿主机内存是8G所以我给虚拟机分配2G内存。
    
 8）网络连接类型的选择，网络连接类型一共有桥接、NAT、仅主机和不联网四种。这里选择桥接模式
   桥接：选择桥接模式的话虚拟机和宿主机在网络上就是平级的关系，相当于连接在同一交换机上。
   NAT：NAT模式就是虚拟机要联网得先通过宿主机才能和外面进行通信。
   仅主机：虚拟机与宿主机直接连起来
 
 9）其余两项按虚拟机默认选项即可
 
 10）创建新虚拟磁盘，磁盘容量大小
    磁盘容量暂时分配80G即可后期可以随时增加，不要勾选立即分配所有磁盘，否则虚拟机会将100G直接分配给CentOS，会导致宿主机所剩硬盘容量减少。
    勾选将虚拟磁盘拆分成多个文件，这样可以使虚拟机方便用储存设备拷贝复制。
    
 11）磁盘名称，默认即可
 
 12）点击自定义硬件，取消不需要的硬件。选择声卡、打印机等不需要的硬件然后移除
 
 13）点击完成，已经创建好虚拟机。
 
 
 接下来安装CentOs
 首先要下载centos镜像文件
 网易镜像地址 http://mirrors.163.com/centos/7/isos/x86_64/
 1）右击刚创建的虚拟机（如CentOs001），选择设置
 
 2）点击CD/DVD，再选择使用ISO映像文件，勾选上启动时连接，选择浏览找到下载好的镜像文件，然后确定。
 
 3）开启虚拟机
 开启虚拟机后会出现以下界面
Install CentOS 7 安装CentOS 7
Test this media & install CentOS 7 测试安装文件并安装CentOS 7
Troubleshooting 修复故障
选择第一项（用上下键选择），安装直接CentOS 7，回车，进入安装

4）选择安装过程中使用的语言，这里选择英文、键盘选择美式键盘。English(united states)点击Continue

5）设置时间，点击时间图标，时区选择上海（直接鼠标点地图上的位置），查看时间是否正确。然后点击Done

6）选择需要安装的软件software selecttion，选择 Server with Gui（图形界面），然后点击Done

7）选择安装位置install actiondestination，在这里可以进行磁盘划分。选择i wil configure partitioning（我将会配置分区），然后点击done
点击加号，选择/boot，给boot分区分200M。最后点击Add,然后以同样的办法给其他区分配好空间后点击Done
然后会弹出摘要信息，点击AcceptChanges(接受更改)

8）设置主机名与网卡信息network & hostname.首先要打开网卡Ethernet(选择ON)，
然后查看是否能获取到IP地址(我这里是桥接)，再更改主机名（如www.shohan.com）后点击Done。

9）最后选择Begin Installation(开始安装)，
设置root密码（ROOT PASSWORD），点击Done，点击USER CREATION 创建管理员用户（如shohan）

10）等待系统安装完毕重启系统即可
要查看虚拟机ip，只需点击（查看）-》（控制台视图），即可看到虚拟机详细信息，这个不准确
还是进入doc界面，输入ifconfig查看本虚拟机ip地址



在图形界面使用 ctrl+alt+F2切换到dos界面  

在命令上 输入 init 5 命令 切换到图形界面 

输入 init 5命令 切换到图形界面

ctrl + alt   退出编辑
ctrl + G     进入编辑

 
 
 
